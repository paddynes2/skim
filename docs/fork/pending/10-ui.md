# Phase 10 pending merge: Ball in my court (UI half)

Built 2026-09-23 by the Phase 10 UI agent, on top of `pending/10.md` (Rust +
wrappers, merge that one too). Fork-owned files written:

    src/fork/court/nudge.ts           pure: parseHhmm, shouldNudgeLocal/Now, ageDays/Label/Tone, nudgeParts
    src/fork/court/prefs.svelte.ts    courtPrefs: fork_court_ai / _ai_cap / _amber_days / _red_days / _nudge / _nudged_at
    src/fork/court/counts.svelte.ts   courtCounts (sidebar badges), refreshed on court:updated
    src/fork/court/store.svelte.ts    courtStore.start(): navHooks.court, counts, nudge timer; courtStore.open(state)
    src/fork/court/CourtNudge.ts      startCourtNudge() minute poll + showCourtNudge() toast
    src/fork/court/CourtRow.svelte    the age + reason badge (amber / red past the settings)
    src/fork/court/SettingsCourt.svelte  settings rows (AI on/off, cap, colours, nudge time + Test)
    src/fork/tests/court-nudge.test.mjs  5 node tests over nudge.ts
    demo/mock/fork-court.ts           ON_ME (6) / WAITING (4) fixtures, forkCourtList / forkCourtCounts

Upstream files edited (each in a marked `Fork (10)` block, nothing else touched):

    src/lib/stores/mail.svelte.ts     imports; court:updated listener; refreshFolders guard; fetchPage routing;
                                      courtStateOf / selectCourt; `mail.courtView` + `mail.selectCourt` exports
    src/components/Sidebar.svelte     "On me" / "Waiting" items with `.count.total` badges, in the same
                                      section as the Phase 7 Calendar item (kept as found)
    src/components/MessageList.svelte header branch for the court views; per-row overlay wrapper with the badge

## mod.rs

(nothing)

## generate_handler

(nothing beyond pending/10.md's three commands)

## commands.rs

(nothing)

## settings ALLOWED

    fork_court_nudged_at   // internal: unix seconds of the last nudge toast shown; written by
                           // courtPrefs.setNudgedAt() so a restart does not re-nudge the same day.
                           // Until allowed, set_setting refuses (caught) and the in-memory value
                           // still prevents a second toast in the running session.

Already listed per pending/10.md and read/written here: `fork_court_ai`,
`fork_court_ai_cap`, `fork_court_amber_days`, `fork_court_red_days`,
`fork_court_nudge`.

## en.json

    {
      "fork.court.sub_on_me": "Oldest first · {n}",
      "fork.court.sub_waiting": "Oldest first · {n} waiting on them",
      "fork.court.age": "{n} days",
      "fork.court.row_label": "On you for {age}",
      "fork.court.row_label_reason": "On you for {age}, {reason}",
      "fork.court.reason_draft": "draft started",
      "fork.court.reason_filed": "filed",
      "fork.court.reason_bulk": "bulk",
      "fork.court.nudge": "On you: {n} · Oldest: {who} ({days}d)",
      "fork.court.nudge_open": "Open",
      "fork.court.settings": "Ball in my court",
      "fork.court.settings_ai": "AI pass on new threads",
      "fork.court.settings_ai_note": "Uses your own key. Off, the rules alone decide what is on you.",
      "fork.court.settings_ai_cap": "Day cap",
      "fork.court.settings_ai_cap_unit": "threads a day",
      "fork.court.settings_colours": "Age colours",
      "fork.court.settings_amber": "Amber after",
      "fork.court.settings_red": "Red after",
      "fork.court.settings_days": "days",
      "fork.court.settings_nudge": "Daily nudge",
      "fork.court.settings_nudge_at": "At",
      "fork.court.settings_nudge_test": "Test"
    }

`fork.nav.on_me` / `fork.nav.waiting` already exist and are used for the
sidebar items, the list header titles, the `g` hint and the palette rows.

## App.svelte

One call after boot, beside the calendar / prep starts (idempotent, returns a stop fn):

    import { courtStore } from "./fork/court/store.svelte";
    ...
    // in the same onMount / after-boot block that calls calendar.start() and startPrepReminder():
    const stopCourt = courtStore.start();
    // and in the teardown, if the block has one:
    stopCourt();

That registers `navHooks.court` (so `g o` / `g w`, the palette rows and the
GoHint entries light up), loads counts, and starts the nudge timer.

## keys

None to add: `src/fork/keys.ts` already routes `g o` -> `navHooks.court("on_me")`
and `g w` -> `navHooks.court("waiting")`; `courtStore.start()` registers the hook.

## SettingsFork.svelte

One mount, after the existing `</section>` of the list settings:

    import SettingsCourt from "./court/SettingsCourt.svelte";
    ...
    <SettingsCourt />

## tauri-core mock

    import { forkCourtCounts, forkCourtList } from "./fork-court";
    ...
    case "fork_court_list":
      return ok(forkCourtList(args.courtState, args.offset ?? 0));
    case "fork_court_counts":
      return ok(forkCourtCounts());
    case "fork_court_recompute":
      return ok(undefined);

Without these the default branch answers `undefined`, `withoutPending` throws
inside `guard`, and the views show "couldn't load" (the honest failure), so
no shot could be taken here.

## fork-shots

    // Phase 10: the court views. Rows carry an age badge ("9d · asks for the deck").
    "court-on-me": {
      phase: "10",
      setup: async (page) => {
        await openInbox(page);
        await page.locator(".sidebar .item", { hasText: "On me" }).click();
        await page.locator(".court-badge").first().waitFor();
        await page.locator('.row:has-text("Contract redline")').first().click();
        await sleep(300);
      },
    },
    "court-waiting": {
      phase: "10",
      setup: async (page) => {
        await openInbox(page);
        await page.locator(".sidebar .item", { hasText: "Waiting" }).click();
        await page.locator(".court-badge").first().waitFor();
        await sleep(300);
      },
    },
    "court-on-me-compact": {
      phase: "10",
      flags: { "skimdemo.fork_density": "compact" },
      setup: async (page) => {
        await openInbox(page);
        await page.locator(".sidebar .item", { hasText: "On me" }).click();
        await page.locator(".court-badge").first().waitFor();
      },
    },
    "court-sidebar-counts": {
      phase: "10",
      setup: async (page) => {
        await openInbox(page);
        await page.locator(".sidebar .item.court .count").first().waitFor();
      },
    },
    "court-nudge-toast": {
      phase: "10",
      // The settings row's Test button fires the toast on demand.
      setup: async (page) => {
        await openInbox(page);
        await page.locator("button", { hasText: "Settings" }).first().click();
        await page.locator("text=Ball in my court").first().scrollIntoViewIfNeeded();
        await page.locator("button", { hasText: "Test" }).click();
        await page.keyboard.press("Escape");
        await page.locator(".toast").waitFor();
        await sleep(200);
      },
    },
    "court-settings": {
      phase: "10",
      setup: async (page) => {
        await openInbox(page);
        await page.locator("button", { hasText: "Settings" }).first().click();
        await page.locator("text=Ball in my court").first().waitFor();
        await page.locator("text=Ball in my court").first().scrollIntoViewIfNeeded();
      },
    },

Not run here (tauri-core cases above are the main session's). Look at the
compact shot first: the badge sits left of the date at `right: 72px`, the
one placement I could not eyeball.

## TOUCHLIST

| File | Symbol | Change | Reason |
|---|---|---|---|
| `src/lib/stores/mail.svelte.ts` | imports | `courtApi`, `COURT_UPDATED`, `VF_ON_ME`, `VF_WAITING`, `courtCounts`, `CourtState` | 10 views |
| `src/lib/stores/mail.svelte.ts` | `attachListeners` | `listen(COURT_UPDATED)`: refreshThreads when a court view is open, `courtCounts.refresh()` always | plan: views live; `mail:updated` names folders, not views |
| `src/lib/stores/mail.svelte.ts` | `refreshFolders` | early return when the selected id is -920/-921, beside the -900 guard | a sync must not bounce an open view to the inbox |
| `src/lib/stores/mail.svelte.ts` | `fetchPage` | route -920/-921 to `courtApi.list(state, offset, limit)` before the `< 0` unified branch | paging, refresh, reading pane, keys, undo unchanged on court rows |
| `src/lib/stores/mail.svelte.ts` | `courtStateOf`, `selectCourt` (new, fork section) | id <-> state; `selectCourt` clears search state then `selectFolder(id)` | same shape as `enterSearch` |
| `src/lib/stores/mail.svelte.ts` | `mail` export | `get courtView`, `selectCourt` | MessageList header, Sidebar selection, courtStore |
| `src/components/Sidebar.svelte` | script + calendar section | two `.item.court` buttons with `.count.total` badges, `class:selected` on -920/-921 | plan: "On me (N)" / "Waiting (N)" |
| `src/components/MessageList.svelte` | header | `{:else if mail.courtView}` branch: title + "Oldest first · N" sub-line + J/K hint; All/Unread/Starred block untouched | the view is its own filter |
| `src/components/MessageList.svelte` | rows | each `MessageRow` wrapped in `.court-wrap` (position: relative); in a court view a `.court-badge-slot` overlay renders `CourtRow` | MessageRow is upstream + fixed-height; an overlay adds no height, so the windowing arithmetic holds |
| `src/components/MessageList.svelte` | script | `nowSecs` minute ticker, `courtOf(row)` | ages recolour across a day boundary without a reload |

## DECISIONS

- **2026-09-23 D-10k (the nudge is the shell's, not notify.rs):** the toast runs in TS (`CourtNudge.ts`, minute poll, `shouldNudgeNow` mirrors `fork::court::should_nudge_local`, text from `fork_court_counts` + the first on-me row). Do NOT also wire the Rust `should_nudge_now` / `nudge_line` into `notify.rs`, or he gets two toasts. Trade-off: no nudge while the window is closed (the tray-only case); if that matters later, move it to Rust and delete `startCourtNudge`.
- **2026-09-23 D-10l (badge is an overlay, not a row edit):** `MessageRow.svelte` is not in this phase's touch list and its height is what the list's windowing measures. The age + reason badge is absolutely positioned inside a wrapper around the row (bottom-right in comfortable, left of the date in compact), so a court row is pixel-identical to an inbox row apart from the badge.
- **2026-09-23 D-10m (no amber token):** `tokens.css` has `--danger` but no warning colour; the badge's amber is `--acct-2` (the amber account swatch every theme defines) rather than a new token, so no contrast run and no shared-file edit.
- **2026-09-23 D-10n (court prefs in their own store):** `fork_court_*` are read through `src/fork/court/prefs.svelte.ts`, not `stores/prefs.svelte.ts` (same reasoning as `calPrefs`: that file is shared). The main session may fold `courtPrefs.hydrate` into `prefs.hydrate` later.
- **2026-09-23 D-10o (a day with nothing on him still counts as nudged):** `startCourtNudge` stamps `nudged_at` before asking for the line, so an empty on-me list does not re-check every minute until midnight. An undo toast on screen defers the nudge to the next minute (one toast at a time, PLAN 3.2).
- **2026-09-23 D-10p (selecting a court view leaves search silently):** `selectCourt` clears `searchQuery` / `searchPrevFolderId` and selects the id, like a sidebar folder click does; it does not go through `exitSearch` (which would first select the previous folder and load a page for nothing).

## migrations

(none)

## Cargo.toml

(none)

## status

Done:
- Views: -920 / -921 through `fetchPage`, `court:updated` listener, `refreshFolders` guard, `selectCourt` / `courtView`; sidebar items + counts; list header branch; per-row age badge (amber/red per settings) with the stored reason mapped to strings.
- `navHooks.court` registered by `courtStore.start()`; `g o` / `g w`, palette rows and GoHint follow from `keys.ts` / `palette.ts` as already written.
- Nudge: `nudge.ts` (pure, tested) + `CourtNudge.ts` (timer + toast, action opens On me) + Test button in settings.
- Settings: `SettingsCourt.svelte` (AI on/off, day cap, amber/red days, nudge time / off, Test).
- Mock: `demo/mock/fork-court.ts`.

Not done (main session, by the rules):
- `App.svelte` `courtStore.start()`, `SettingsFork.svelte` mount, `en.json` strings, `tauri-core.ts` cases, `fork-shots.mjs` scenarios, `fork_court_nudged_at` in settings ALLOWED (all exact snippets above).
- No PNGs: the mock cases live in a main-session file, so the views cannot be rendered in the harness from here.

Gates (this agent, 2026-09-23):
- `npm run check`: 0 errors, 30 warnings, all in `src/fork/calendar/EventPanel.svelte` (Phase 7 agent's file, `state_referenced_locally`), 359 files.
- `npm run build`: built in 4.6 s.
- `node --test src/fork/tests/court-nudge.test.mjs`: 5 passed, 0 failed.
