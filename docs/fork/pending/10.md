# Phase 10 pending merge: Ball in my court (Rust + api wrappers)

Built 2026-09-23 by the Phase 10 agent. Fork-owned files written:
`src-tauri/src/fork/court.rs` (was a stub), `src-tauri/src/fork/migrations/f0005_court.sql`
(was a placeholder, already in `FORK_MIGRATIONS`), `src/fork/court/api.ts`,
`src/fork/court/types.ts`, this file. The sidebar views, the row age colouring,
the `g o` / `g w` keys, the toast scheduling and the shots are a later agent's:
nothing under `src/` except the two wrapper files was touched.

## mod.rs

    pub mod court;

(already pre-declared; nothing to add)

## generate_handler

    fork::court::fork_court_list,
    fork::court::fork_court_counts,
    fork::court::fork_court_recompute,

## commands.rs

(nothing: the commands live in `fork/court.rs`)

## settings ALLOWED

(nothing to add: all four Phase 10 keys are already listed)

Read by `fork::court` through `db::queries::get_setting`:

    fork_court_ai        // "on" turns the AI pass on; anything else (or unset) = off
    fork_court_ai_cap    // threads/day the AI pass may judge; default 200 (AI_CAP_DEFAULT)

Not read here (the view / toast agent reads them): `fork_court_amber_days`,
`fork_court_red_days`, `fork_court_nudge`.

Internal row, written and read only by Rust, NOT to be added to ALLOWED:
`fork_court_ai_used` = `"YYYY-MM-DD:N"`, the day's AI counter.

## en.json

(none from this half: the strings are the view agent's. Stored `reason`
values are English literals the row may show as-is or map to keys:
`"draft started"`, `"bulk: list-unsubscribe"`, `"bulk: automated sender"`,
`"filed"`, plus the AI's free-text reason, at most 8 words.)

## App.svelte

(nothing from this half)

## keys

(none from this half; the plan's `g o` / `g w` are the view agent's)

## tauri-core mock

Cases for `demo/mock/tauri-core.ts` (the view agent will want fixtures; the
wire shapes are):

    case "fork_court_list":      // args: { courtState: "on_me"|"waiting", offset, limit }
      return ok([...ThreadRow, since: <unix>, reason: string|null][]);   // oldest `since` first
    case "fork_court_counts":    // no args
      return ok({ onMe: number, waiting: number });
    case "fork_court_recompute": // no args
      return ok(undefined);

Event: `"court:updated"` (payload `{}`), after any pass that moved a row.

## fork-shots

(none from this half: no UI)

## TOUCHLIST

| File | Symbol | Change | Reason |
|---|---|---|---|
| `src-tauri/src/commands/ai.rs` | `AiContext` | `struct` -> `pub(crate) struct`; fields `endpoint`, `key`, `model`, `now` -> `pub(crate)` (`provider`, `locale` stay private) | 10 AI pass: the plan says "uses `ai_context`"; the batch request in `fork::court::complete` resolves provider / key / model exactly as every AI command does instead of duplicating that 40-line resolution |
| `src-tauri/src/commands/ai.rs` | `ai_context` | `async fn` -> `pub(crate) async fn` | same |

Wiring the MAIN SESSION must add (I did not edit these files):

| File | Symbol | Change | Reason |
|---|---|---|---|
| `src-tauri/src/fork/mod.rs` | `start` | `crate::fork::court::full_pass(app.clone(), db.clone());` once (needs the `Db`: `app.state::<AppState>().db.clone()`), and listen for `mail:updated`: `app.listen("mail:updated", move \|e\| { let touched = serde_json::from_str::<serde_json::Value>(e.payload()).ok().and_then(\|v\| v.get("folderId").and_then(\|f\| f.as_i64())).map(\|f\| vec![f]).unwrap_or_default(); crate::fork::court::on_mail_updated(app2.clone(), db2.clone(), touched); })` | plan: "on every `mail:updated` for touched threads and a full pass at startup". `sync.rs:1255` emits `{ folderId }`; every other emitter sends `{}`, which `on_mail_updated` treats as "re-examine everything" (one query + a hash-map walk, then only changed rows are written). If the listener turns out too chatty, debounce it in `start` (a 2 s `tokio::time::sleep` coalescer); the pass itself is idempotent |
| `src-tauri/src/lib.rs` | `generate_handler!` | the three commands above | |
| `src-tauri/src/notify.rs` (or `fork/mod.rs`) | daily nudge | at the setting's time (`fork_court_nudge`, default "09:00", "off" disables), `if fork::court::should_nudge_now(&setting, last_nudged_at, now) { if let Some(text) = fork::court::nudge_line(conn)? { toast(text, opens VF_ON_ME) ; store last_nudged_at } }`. `last_nudged_at` is the main session's to persist (suggest a `fork_court_nudged_at` settings row, internal). Both helpers are tested; `should_nudge_local` is the clock-free core | plan: "daily toast at 09:00 local (setting) ... click opens On me" |

## DECISIONS

- **2026-09-23 D-10a ("filed" = not in the inbox):** the plan's "thread only in folders he filed as FYI/marketing" is read as: a thread with no copy in a folder of role `inbox` is filed (`reason: "filed"`, state `none`). Role NULL (user labels), `archive`/all-mail, `sent`, `important`, `starred` do not count as "in play" on their own: an archived thread is one he dealt with, and Gmail keeps Important/Starred copies after archiving. A label copy PLUS an inbox copy (Gmail) is in play. Junk-only / trash-only threads are `none` whichever side wrote last.
- **2026-09-23 D-10b (waiting has no bulk / inbox test):** a thread whose last message is from one of his addresses is `waiting` wherever it lives (Sent has no inbox copy by construction). Only junk/trash-only threads are excluded. This means every outbound mail ever is "waiting" until answered; the views sort oldest first, so the tail is old. If that is noise in practice, a `since` floor (e.g. 90 days) belongs in the view / a setting, not in the classification.
- **2026-09-23 D-10c (own addresses):** "one of the account's own addresses" = every `accounts.email` and every `imap_user` containing `@`, across ALL accounts, lowercased. Mail from one of his mailboxes to another is still from him.
- **2026-09-23 D-10d (draft rule wins):** an unsent local draft (`drafts` row whose `reply_to_message_id` or `origin_message_id` is a message of the thread) makes the thread `on_me`, reason `draft started`, `needs_reply = 1`, even over bulk / filed / waiting. Rows in `drafts` are unsent by definition (sending deletes them). Server-draft copies in a folder of role `drafts` are skipped when picking the last message (a draft in flight is not the last word).
- **2026-09-23 D-10e (never re-classify, precisely):** the row is left alone when its `last_message_id` is unchanged AND the deterministic verdict (state, since, reason) is unchanged. Same `last_message_id` but a moved verdict (draft opened / closed, thread archived) updates state / since / reason and keeps the AI columns (`needs_reply` only overwritten when the rule itself asserts one, i.e. the draft rule). A new `last_message_id` rewrites the row and resets `model` / `needs_reply` to NULL, which is what makes the thread an AI candidate again. `fork_court_recompute` (forced) rewrites everything and resets every AI verdict.
- **2026-09-23 D-10f (AI candidates = `state = 'on_me' AND model IS NULL`):** no extra column: `model` doubles as "judged at this last_message_id". The view predicate is `state = 'on_me' AND coalesce(needs_reply, 1) = 1`, so an AI "no reply needed" hides the row and the deterministic result shows until the AI has spoken. A batch whose reply does not parse keeps the deterministic verdict but is still stamped with the model (it spent the cap; not retried until the thread changes). A provider / network error leaves the batch unstamped for the next pass. The AI pass runs only after a deterministic pass that wrote something, so an idle mailbox costs nothing.
- **2026-09-23 D-10g (`fork_court_ai` default OFF):** BYOK and optional in the plan; nothing calls a model until Patrick turns it on in settings. The day cap counts threads sent, in local days, in the internal `fork_court_ai_used` row.
- **2026-09-23 D-10h (sender pattern by hand):** the crate has no `regex`; `no-?reply|notifications?|mailer-daemon|bounce` is four case-insensitive substring tests on the whole address (`is_automated_sender`). "bounce" matches `bounces.thomas@` too; accepted, same as the plan's regex would.
- **2026-09-23 D-10i (command parameter name):** `fork_court_list` takes `court_state` (wire: `courtState`), not `state`, because `state: State<'_, AppState>` is the Tauri parameter every command already has. `courtApi.list(state, offset, limit)` hides this.
- **2026-09-23 D-10j (`mail:updated` payload):** the hook takes `touched_folder_ids: Vec<i64>`; the sync emits at most one `folderId`, the rest emit `{}`. An empty list = full scope. Threads whose messages all vanished are swept by the full scope (`thread not in threads`) and by `Scope::Threads`; a folder scope cannot name them, which the next `{}` event covers.

## migrations

    src-tauri/src/fork/migrations/f0005_court.sql   (fork_court, exactly the plan's columns + idx_fork_court_state_since)

## Cargo.toml

(none)

## status

Done (Rust + wrappers):
- `f0005_court.sql`: `fork_court(thread_id PK, account_id, state CHECK(on_me|waiting|none), since, last_message_id, needs_reply, reason, model, computed_at)` + index; the shipped-migrations test in `fork::db` covers it.
- `fork::court::pass(conn, &Scope, forced)`: the deterministic pass (last message by date deduped by Message-ID like `get_thread`, own-address -> waiting, bulk rules, filed rule, draft rule, unchanged rows untouched, gone threads swept). `on_mail_updated(app, db, touched_folder_ids)` / `full_pass(app, db)` spawn it and emit `court:updated`; `fork_court_recompute` forces it.
- `fork::court::ai_pass(db)`: `fork_court_ai` on/off, `fork_court_ai_cap` per local day, batches of 20, strict JSON `[{thread_id, needs_reply, reason}]` (fence-tolerant), parse failure keeps the deterministic verdict, one-shot completion over `anthropic::stream` / `openai_compat::stream` collected into a String via the shared `ai_context`. Never sends mail; never called from tests.
- Commands: `fork_court_list(courtState, offset, limit) -> CourtRow[]` (`ThreadRow` flattened + `since` + `reason`, shaped by the thread's latest message like `list_threads`, oldest `since` first), `fork_court_counts() -> {onMe, waiting}`, `fork_court_recompute()`.
- Nudge helpers: `nudge_line(conn) -> Option<String>` ("On you: N · Oldest: X (5d)"), `should_nudge_now(setting_hhmm, last_nudged_at, now)` + clock-free `should_nudge_local`, `age_days`.
- `src/fork/court/{api,types}.ts`: `courtApi.list/counts/recompute`, `COURT_UPDATED`, `VF_ON_ME`/`VF_WAITING`, `CourtRow`, `CourtCounts`, `CourtState`.
- Tests (16 in `fork::court`): last-message + dedup across folder copies, own-address (case, imap_user), each bulk rule (list-unsubscribe, every sender pattern, filed / junk / trash / label+inbox), draft rule (local reply draft, removal, server-draft copy skipped, origin link), AI reset on a new message + forced rewrite, folder / thread scopes + row sweep, list shape + serialized JSON shape + paging + oldest-first, counts, nudge line, JSON parse + fallbacks, cap / counter / enabled settings, AI candidate query, `should_nudge_local` / `should_nudge_now`, `age_days`. All against `Db::open_in_memory()`.

Not done (by design, other agents / main session):
- Sidebar views, row age colours, keys, toast scheduling, mock cases, shots, en.json strings, the `generate_handler!` / `fork::start` wiring (exact snippets above).
- The AI pass has no live run here (no key in this environment, and the rules forbid network in tests): the request/parse/write path is unit-tested around the network call, the call itself is the same `stream` every AI command uses.

Gate results (this agent, 2026-09-23, last run after every other agent's
compile errors in `fork/mcp/`, `fork/scheduler.rs`, `fork/crm.rs`,
`fork/smell.rs` had cleared):
- `cargo fmt --check`: clean for `court.rs`.
- `cargo clippy --all-targets -- -D warnings`: exit 0 (whole crate).
- `cargo test` (whole suite): 493 passed, 2 failed. Both failures are
  `fork::compose::tests::{mime_is_alternative_with_the_text_part_equal_to_body_text,
  no_html_is_text_plain_as_before}` (Phase 6 MIME assembly: CRLF vs LF in the
  text part, and an HTML part made up when none was given). Not this phase's
  files; `fork::court` is not referenced from `compose.rs`.
- `cargo test fork::court`: 16 passed, 0 failed.
- `fork::db::repair_tests::f0001_and_f0002_repair_live_rows` failed on the
  first run against my migration (it re-runs every fork migration from version
  0): fixed by `CREATE TABLE IF NOT EXISTS` / `CREATE INDEX IF NOT EXISTS`,
  matching f0004 / f0006. Passes now.
- `npm run check`: 0 errors, 0 warnings (334 files).
