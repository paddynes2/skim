# Phase 6 pending merge: Compose (6.1 send key + ✕, 6.2 inline reply, 6.3 rich text, 6.4 one scheduler)

Built 2026-09-23 by the Phase 6 agent (6.5 is another agent's). New fork-owned
files: `src-tauri/src/fork/compose.rs`, `src-tauri/src/fork/scheduler.rs`
(filled), `src-tauri/src/fork/migrations/f0003_op_schedule.sql` +
`f0006_draft_html.sql` (filled), `src/fork/RichEditor.svelte`,
`src/fork/compose/{api.ts,html.ts,when.ts,inline.svelte.ts,SendLater.svelte,SendHeld.svelte}`,
`src/fork/scheduled/ScheduledList.svelte`, `src/fork/tests/{when,compose-html}.test.mjs`,
`demo/mock/fork-compose.ts`, `demo/fork-shots-6.mjs` (my stand-in runner; delete
once the scenarios below are in `fork-shots.mjs`), shots in `docs/fork/shots/6/`.

## mod.rs

    pub mod compose;

NOTE: I added this single line to `src-tauri/src/fork/mod.rs` myself (after
`pub mod commands;`) so `commands/compose.rs::send_draft` (which calls
`crate::fork::scheduler::hold/announce`) and `fork::scheduler` compile
together with `fork::compose`. Keep it if you regenerate `mod.rs`.
`pub mod scheduler;` was already there.

Also needed in `fork::start` (fork/mod.rs, main session):

    pub fn start(app: AppHandle) {
        scheduler::start(app.clone());
    }

Until that line lands, a held send fires only when something else runs the
engine's ops (a sync, another op); nothing is lost, the table outlives it.

## generate_handler

    fork::compose::fork_draft_html_get,
    fork::compose::fork_draft_html_set,
    fork::scheduler::fork_cancel_scheduled,
    fork::scheduler::fork_scheduled_list,
    fork::scheduler::fork_send_now,

## commands.rs

(nothing)

## settings ALLOWED

Already present in `fork/settings.rs` (Phase 6 block), read through
`prefs.replyInline` / `prefs.richText` / `prefs.undoSendSecs`; nothing added:

    fork_reply_inline    // on | off, default on   (6.2)
    fork_rich_text       // on | off, default on   (6.3)
    fork_undo_send_secs  // 0 | 5 | 10 | 20 | 30, default 10 (6.4)

`SettingsFork.svelte` (main-session file) has no controls for these three yet.

## en.json

    {
      "fork.compose.formatting": "Formatting",
      "fork.compose.bold": "Bold",
      "fork.compose.italic": "Italic",
      "fork.compose.underline": "Underline",
      "fork.compose.link": "Link",
      "fork.compose.link_placeholder": "Paste or type a link, then Enter",
      "fork.compose.bullets": "Bulleted list",
      "fork.compose.numbers": "Numbered list",
      "fork.compose.quote": "Quote",
      "fork.compose.clear_format": "Clear formatting",
      "fork.compose.edit_quoted": "Edit quoted text",
      "fork.compose.edit_signature": "Edit signature",
      "fork.compose.pop_out": "Open in a window",
      "fork.compose.discard_confirm": "Discard?",
      "fork.compose.send_later": "Send later",
      "fork.compose.when_tomorrow": "Tomorrow morning",
      "fork.compose.when_monday": "Monday morning",
      "fork.compose.when_in2h": "In 2 hours",
      "fork.compose.when_pick": "Pick…",
      "fork.compose.when_placeholder": "3d · tomorrow 9am · fri 14:00",
      "fork.compose.when_invalid": "Not a time I understand",
      "fork.compose.schedule": "Schedule",
      "fork.compose.sending": "Sending…",
      "fork.compose.undo": "Undo",
      "fork.compose.scheduled_for": "Scheduled for {when}",
      "fork.scheduled.title": "Scheduled",
      "fork.scheduled.empty": "Nothing scheduled.",
      "fork.scheduled.no_recipient": "(no recipient)",
      "fork.scheduled.edit": "Edit",
      "fork.scheduled.edit_hint": "Cancel the schedule and reopen the draft",
      "fork.scheduled.send_now": "Send now",
      "fork.scheduled.note": "Scheduled mail leaves from this computer: keep Skim running (the tray is fine)."
    }

Also used: `fork.reading.show_quoted` / `fork.reading.hide_quoted` (Phase 5's
keys, on the composer's "•••" pill) and `fork.row.no_subject` (exists).

## App.svelte

1. **R / A / F go inline (6.2).** Replace the body of `replyToSelected`:

        import { replyInlineOrWindow } from "./fork/compose/inline.svelte";
        ...
        async function replyToSelected(mode: "reply" | "reply_all" | "forward" = "reply") {
          // Fork (6.2): inline under the open message when the setting is on and
          // a thread is open; otherwise the window, exactly as before.
          await replyInlineOrWindow(mode);
        }

   (`replyInlineOrWindow` falls back to the old getThread → latest →
   getReplyTemplate → openComposeWindow path when `prefs.replyInline` is off or
   nothing is open.) `isTyping()` already covers the inline editor
   (contenteditable) so J/K etc. stay quiet while it has focus.

2. **Undo-send toast (6.4).** Next to `<Toast />` (line ~521):

        import SendHeld from "./fork/compose/SendHeld.svelte";
        ...
        <Toast />
        <SendHeld />

   It listens for the Rust event `fork:send-held` (sent from any window, so a
   send from a compose window shows "Sending… Undo" in the main window too).

3. **Scheduled folder (6.4), id -910.** The mail store is not touched; the list
   is self-contained. Three snippets:

   `src/lib/stores/mail.svelte.ts` `fetchPage`, first line inside the function
   (so a `mail:updated` refresh on this folder never hits a real query):

        import { SCHEDULED_FOLDER_ID } from "../../fork/compose/api";
        ...
        if (folderId === SCHEDULED_FOLDER_ID) return Promise.resolve([]);

   `src/components/Sidebar.svelte`, under the main folders (after Drafts is the
   natural spot; same markup as a role folder, no unread count):

        import { SCHEDULED_FOLDER_ID } from "../fork/compose/api";
        ...
        <button class="item" class:active={mail.selectedFolderId === SCHEDULED_FOLDER_ID}
                onclick={() => void mail.selectFolder(SCHEDULED_FOLDER_ID)}>
          <span class="icon">…clock svg…</span>
          <span class="name">{t("fork.scheduled.title")}</span>
        </button>

   (`mail.selectFolder(-910)` tolerates an id absent from `mail.folders`: it
   sets `selectedFolderId`, hands `folderSelected` an `undefined` find, clears
   the selection and calls `fetchPage`, which the line above short-circuits;
   read at `mail.svelte.ts:415-429`.)

   `App.svelte` panes, in place of the list + pane when the folder is selected:

        import ScheduledList from "./fork/scheduled/ScheduledList.svelte";
        import { SCHEDULED_FOLDER_ID } from "./fork/compose/api";
        ...
        <Sidebar />
        {#if mail.selectedFolderId === SCHEDULED_FOLDER_ID}
          <ScheduledList />
        {:else}
          <MessageList />
          ...existing third-pane branches...
        {/if}

4. **`sync.rs` HTML wiring (6.3), main session only** (my sync.rs allowance was
   the drain query). In `execute_send` and `execute_save_draft`, replace the
   `smtp::build_message(` call with:

        // Fork (6.3): the HTML rendition, when the rich editor wrote one.
        let html = crate::fork::compose::outgoing_html(&self.db, draft_id).await;
        let raw = smtp::build_message_with_html(
            &self.account, &draft, &refs, &attachments,
            imap_message_id.as_deref() /* Some(&imap_message_id) in save_draft */,
            false /* true in save_draft */,
            html.as_deref(),
        )?;

   Without this, rich drafts are stored and edited as rich text but every
   message still leaves as text/plain (`build_message` = `..._with_html(None)`).

## keys

(none new: R / A / F keep their bindings in App.svelte and re-route through
`replyInlineOrWindow`; Ctrl+Enter, Esc, Ctrl+B/I/U/K, Ctrl+Shift+7/8 are handled
inside the composer / editor, never at the app level.)

## tauri-core mock

`demo/mock/fork-compose.ts` exports `forkComposeInvoke(cmd, args)` covering
`fork_draft_html_get/set`, `send_draft` (returns an op id, records a hold),
`fork_scheduled_list` (three fixtures: in 2 h, "Tomorrow 08:00", "Mon 08:00"),
`fork_cancel_scheduled`, `fork_send_now`. Wire in `demo/mock/tauri-core.ts`:

    import { forkComposeInvoke } from "./fork-compose";
    ...
    export function invoke<T = any>(cmd: string, args: any = {}): Promise<T> {
      if (AI_COMMANDS.has(cmd)) { ... }
      // Fork (6): compose + scheduler commands.
      const fc = forkComposeInvoke(cmd, args);
      if (fc) return Promise.resolve(fc.value as T);
      ...

(The `send_draft` case must come before the `default:` arm; the helper takes
it, so nothing else changes.)

## fork-shots

All phase "6". `openInlineReply` = `openHero` + click the pane footer's Reply
button + wait for `.inline-reply .compose-form`. Bodies are in
`demo/fork-shots-6.mjs` `SCENARIOS`, copy them verbatim:

    "inline-reply-open":    inline reply under the focused message, AI bar showing
    "rich-editor-toolbar":  words typed with a bold run, quote unfolded
    "send-later-menu":      the split Send caret open, "Pick…" with "fri 14:00"

and, once App mounts the list:

    "scheduled-list": { phase: "6", setup: async (page) => {
      await openInbox(page);
      await page.locator(".sidebar .item", { hasText: "Scheduled" }).click();
      await page.locator(".scheduled .row").first().waitFor();
    } },

Shots taken (4 themes each) in `docs/fork/shots/6/` for the first three; the
strings show as raw `fork.*` keys until en.json lands. `scheduled-list` not
taken (needs the App.svelte mount).

## TOUCHLIST

| File | Symbol | Change | Reason |
|---|---|---|---|
| `src/components/ComposeForm.svelte` | props | `variant?: "default" \| "reply"`, `onPopOut?` | 6.2 inline reply |
| `src/components/ComposeForm.svelte` | script: rich block after `canPickFrom` | `richMode`, `wordsHtml`/`wordsText`/`tail`, `tailParts`, `onWordsChange`, `onTailInput`, body-change `$effect`, `flushHtml()`; `canPickFrom` also `variant !== "reply"` | 6.3 data model (D5) |
| `src/components/ComposeForm.svelte` | `discardClick()` + `discardArmed` | two-click Discard | 6.1 |
| `src/components/ComposeForm.svelte` | load `$effect` | reads `fork_draft_html`, splits the tail; reply variant scrolls into view + focuses | 6.2, 6.3 |
| `src/components/ComposeForm.svelte` | `scheduleSave` | `await flushHtml()` after `updateDraft` | 6.3 |
| `src/components/ComposeForm.svelte` | `send(when?)` | flushes HTML; `api.sendDraft(id, notBefore, label)` with the undo hold (`prefs.undoSendSecs`) or the picked time | 6.4 |
| `src/components/ComposeForm.svelte` | `sendKey`, `onFormKeydown`, `popOut`, `close`, `onDestroy` | Ctrl/Cmd+Enter sends; Esc on the reply variant; pop-out; window ✕ saves when dirty; reply teardown deletes an untouched draft | 6.1, 6.2 |
| `src/components/ComposeForm.svelte` | template | root gets `bind:this`, `class:reply`, `onkeydown`; subject `onkeydown={sendKey}`; body = `<RichEditor>` + `.tail` (sig, "•••" quote, "Edit quoted text") or the textarea; footer = split Send (`SendLater`), pop-out button, Discard for both variants | 6.1-6.4 |
| `src/components/ComposeForm.svelte` | style | `.discard.armed`, `.compose-form.reply*`, `.popout`, `.send-split`, `.tail*`, `.quote-*` | |
| `src/components/ReadingPane.svelte` | imports, `reply()`, target `$effect`, `inlineReplySlot` snippet rendered after the focused / shown `messageBlock`, `.inline-reply` style | inline reply under the open message | 6.2 |
| `src-tauri/src/commands/compose.rs` | `send_draft` | `+ not_before: Option<i64>, label: Option<String>` → `Result<i64>` (op id); `fork::scheduler::hold` inside the enqueue transaction, `announce` instead of `run_ops` when held | 6.4 |
| `src-tauri/src/mail/smtp.rs` | `build_message` → `build_message_with_html(.., html: Option<&str>)` | `build_message` delegates with `None`; `Some` = `multipart/alternative[text/plain, text/html]`, inside `multipart/mixed` with attachments | 6.3 |
| `src-tauri/src/mail/sync.rs` | `drain_ops` SELECT | `AND id NOT IN (SELECT op_id FROM fork_op_schedule WHERE not_before > unixepoch())` (already in HEAD d44ef77) | 6.4 (D4) |
| `src-tauri/src/fork/mod.rs` | module list | `pub mod compose;` (main-session file; one line, see above) | 6.3 |
| `src/lib/api.ts` | `sendDraft` | `(draftId, notBefore = null, label = null) => invoke<number>` | 6.4 |
| `src/lib/types.ts` | – | not touched (`Draft` unchanged; `ScheduledSend` lives in `src/fork/compose/api.ts`) | |
| `package.json`, `package-lock.json` | dependencies | `squire-rte ^2.4.9` (MIT). npm (older than the lock's) also dropped the `libc` arrays from 18 optional-dep entries and moved the root version to 1.1.0 in the lock; restore the lock from HEAD and re-add only the squire entry if you want a minimal diff | 6.3 |

## DECISIONS

- **2026-09-23 D-6a (6.3, editor block model):** one `<div>` per line, `<div><br></div>` for a blank line (Squire's default), not `<p>` per paragraph. `textToHtml`/`htmlToText` are exact inverses over that model, so `body_text` and the editor never drift by a line: the AI stream (`draft.body = streamed + tail`) re-renders through the same path, and `splitTail` keeps finding the signature and the quote.
- **2026-09-23 D-6b (6.1, where Discard lives):** in the footer bar next to Save, not in `WindowControls` (upstream, not named by the phase). Second click within 3 s discards; the first turns the icon into "Discard?".
- **2026-09-23 D-6c (6.3, "Edit quoted text"):** turns the tail (signature block + quote) into a plain textarea under the rich editor; the words stay rich. `body_text = wordsText + tail` in both modes.
- **2026-09-23 D-6d (6.3, the signature in HTML):** emitted only while its `\n\n-- \nsig` block is still in `body_text`, so a sign-off the user deleted never comes back in the HTML part. Inline `cid:` images of the quoted original are dropped (they would point at the app's own protocol).
- **2026-09-23 D-6e (6.3, HTML at send time is a main-session touch):** `fork::compose::outgoing_html(db, draft_id)` exists for `execute_send`/`execute_save_draft`; the two call-site edits are listed under App.svelte §4 because sync.rs was another agent's beyond the drain query.
- **2026-09-23 D-6f (6.4, one task):** a single scheduler task over every account (`plan()` returns the accounts with a due hold); the plan's "that account's engine" is honoured per account inside it. Sleep is capped at 1 h and re-planned, so a clock jump costs at most an hour.
- **2026-09-23 D-6g (6.4, undo toast):** shown from the Rust event `fork:send-held` in the main window (a compose window has no toast host and closes on send). The toast lasts `hold - 1 s`; a hold longer than 60 s (send later) gets a 5 s "Scheduled for …" toast with Undo instead. `Z` is not wired to it (the undo stack's `pushFlag` would leave a stale entry past the margin).
- **2026-09-23 D-6h (6.2, teardown):** an inline reply unmounted by a thread switch deletes its draft when untouched and keeps it (flushServer) when edited, mirroring Esc.
- **2026-09-23 D-6i (6.4, refusal semantics):** `cancel` refuses when `not_before - now < 1` OR the op is no longer `pending`; the list hides holds whose draft is gone.

## migrations

    f0003_op_schedule.sql   fork_op_schedule(op_id PK → pending_ops ON DELETE CASCADE, not_before, kind, label) + index
    f0006_draft_html.sql    fork_draft_html(draft_id PK → drafts ON DELETE CASCADE, words_html, updated_at)

## Cargo.toml

(none)

## status

Done:
- 6.1: Ctrl/Cmd+Enter in subject / body / rich editor / tail sends; window ✕ saves an edited draft (`save()`) and deletes an untouched one; two-click Discard in both variants.
- 6.2: `ComposeForm variant="reply"` inline under the focused (or shown) message; From picker hidden; pop-out (saves, opens the window, unmounts); scroll + focus; Esc = discard when untouched / keep when edited; `inlineReply` store + `replyInlineOrWindow` for App.svelte; `prefs.replyInline` honoured.
- 6.3: `squire-rte` 2.4.9; `RichEditor.svelte` (toolbar, Ctrl+B/I/U/K, Ctrl+Shift+7/8, paste sanitised via `sanitizeToFragment`, images still become attachments through the existing `onPaste`); `fork_draft_html` + `fork_draft_html_get/set`; `outgoing_html` assembling words + `skim-sig` + `gmail_quote/blockquote cite`; `build_message_with_html`; 8 Rust tests (MIME alternative with text == body_text, mixed over alternative with attachments, plain path byte-identical, sanitising, cascade).
- 6.4: `fork_op_schedule`, drain predicate (in HEAD), `fork::scheduler` (hold / plan / cancel / send_now / list / start task with Notify re-arm), `send_draft(not_before, label) -> op id`, `SendLater.svelte` (presets + Pick with date/time + free text), `when.ts` parser, `SendHeld.svelte` toast, `ScheduledList.svelte`; 6 Rust tests (drain skips held, takes the ones behind; cancel margin; plan / sleep; list + holds; sync.rs carries the predicate verbatim) + 7 node tests for the parser + 1 for textToHtml.

Not done / needs the main session:
- Everything under "## App.svelte" (R/A/F re-route, `<SendHeld />`, Scheduled folder entry + panes + `fetchPage` line, the two `sync.rs` HTML call sites), `fork::start` calling `scheduler::start`, `generate_handler`, en.json, mock wiring, `fork-shots.mjs` scenarios. Until those land: keys still open a window, held sends fire only on the next engine drain, rich drafts still leave as text/plain, the Scheduled folder is unreachable, the undo toast never shows.
- Settings UI for `fork_reply_inline` / `fork_rich_text` / `fork_undo_send_secs` (`SettingsFork.svelte` is main-session).
- `htmlToText` is only exercised in the browser (needs a DOM); no node test.
- The `scheduled-list` shot.

Gate results (this agent, 2026-09-23):
- `npm run check`: 0 errors; 30 warnings, all in `src/fork/calendar/EventPanel.svelte` (Phase 7 agent), 0 in Phase 6 files.
- `cargo fmt --check`: clean.
- `cargo clippy --all-targets -- -D warnings`: clean (an earlier run failed on `fork/smell.rs`, the 6.5 agent's file; it passed on the final run).
- `cargo test` (whole suite): 495 passed, 0 failed.
- `node --test src/fork/tests/*.test.mjs`: 11 passed.
- Shots: `docs/fork/shots/6/{inline-reply-open,rich-editor-toolbar,send-later-menu}-{4 themes}.png`, looked at; one defect found and fixed from them (the editor rebuilt on every keystroke and typed backwards).
